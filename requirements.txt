# iGFT runtime dependencies.
#
# 1. Install PyTorch first (pick the wheel matching your CUDA version), e.g.:
#      pip install torch --index-url https://download.pytorch.org/whl/cu126
# 2. Then install the remaining dependencies:
#      pip install -r requirements.txt
#
# A fully pinned environment snapshot is available in requirements-lock.txt.

numpy
pandas
tqdm
scikit-learn
rank-bm25

beir>=1.0.1
sentence-transformers>=2.2.2
transformers>=4.29.0
torch>=1.13.1

# Used by SPTAR dense retrieval / faiss indexing
faiss-cpu
pytrec-eval-terrier
